# Simple Python Nmap Scanner

## Overview
I wrote this script to automate my daily reconnaissance tasks. Instead of typing Nmap commands manually every time, I use this script to scan a target and save the open ports to a text file automatically.

It uses the `python-nmap` library.

## How to use
1. Install the library:
   ```bash
   pip install python-nmap